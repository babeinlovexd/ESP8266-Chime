esphome:
  name: test-chime
  build_path: build

wifi:
  ssid: "test"

logger:
  baud_rate: 0

esp8266:
  board: d1_mini
  framework:
    version: recommended

external_components:
  - source:
      type: local
      path: components

esp8266_chime:
  id: my_chime
  bclk: GPIO15
  ws: GPIO2
  dout: GPIO3
  sd:
    number: GPIO12
    inverted: false

number:
select:
button:
switch:
